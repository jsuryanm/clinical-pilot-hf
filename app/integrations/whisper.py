"""Whisper transcription. OWNER: Person A — Day 2.

Use HF inference endpoint for the hackathon, document a local fallback.
"""

from __future__ import annotations

from pathlib import Path


def transcribe(audio_path: Path, language: str = "en") -> str:
    """TODO(Person A): Whisper large-v3 via HF inference."""
    raise NotImplementedError("Person A — Day 2")
